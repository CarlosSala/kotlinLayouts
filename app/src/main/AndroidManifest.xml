<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DynamicFragmentActivity" />
        <activity android:name=".StaticsFragmentsActivity" />
        <activity android:name=".DesignAdaptable2Activity" />
        <activity android:name=".TextInputLayoutActivity" />
        <activity android:name=".ScrollViewActivity" />
        <activity android:name=".TableLayoutActivity" />
        <activity android:name=".DesignAdaptableActivity" />
        <activity
            android:name=".FrameLayoutActivity"
            android:exported="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>